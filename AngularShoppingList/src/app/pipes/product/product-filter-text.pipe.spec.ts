import { ProductFilterTextPipe } from './product-filter-text.pipe';

describe('ProductFilterTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductFilterTextPipe();
    expect(pipe).toBeTruthy();
  });
});
